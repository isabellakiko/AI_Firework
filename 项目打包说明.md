# 项目打包说明

## 📦 打包信息

- **打包日期**：2025-10-29
- **打包方式**：Mac 压缩（包含完整 Git 历史）
- **压缩前大小**：约 260 MB（已清理）
- **原始大小**：3.1 GB

---

## 🗑️ 已删除的内容

为减小传输文件大小，已删除以下**可恢复**的文件：

### 1. `web/node_modules/` - 1.2 GB
- **内容**：前端 npm 依赖包（1733个包）
- **恢复方法**：运行 `cd web && pnpm install`

### 2. `web/.next/` - 1.1 GB
- **内容**：Next.js 构建缓存
- **恢复方法**：运行 `cd web && pnpm dev` 会自动生成

### 3. `docker/volumes/` - 625 MB
- **内容**：Docker 数据卷（数据库、插件缓存等）
- **恢复方法**：运行 `cd docker && docker compose up -d` 会自动创建

**总节省**：约 2.9 GB

---

## ✅ 保留的内容

- ✅ **`.git/`** - 完整的 Git 历史（所有提交记录）
- ✅ **`web/`** - 前端源代码
- ✅ **`api/`** - 后端 API 代码
- ✅ **`docker/`** - Docker 配置文件
- ✅ **`dev-notes/`** - 所有开发文档和修改记录
- ✅ **`package.json`** - 依赖清单
- ✅ **`pnpm-lock.yaml`** - 依赖锁定文件

---

## 🚀 收到项目后的快速启动

### 步骤 1：解压项目
```bash
# 解压到指定目录
unzip dify-brand-customization.zip -d ~/projects/
cd ~/projects/01_测试前端分离
```

### 步骤 2：恢复前端依赖
```bash
cd web
pnpm install
# 安装时间：约 2-5 分钟
```

### 步骤 3：启动开发环境

**选项 A：本地前端开发（推荐）**
```bash
# 终端 1：启动 Docker 后端
cd docker
docker compose up -d

# 终端 2：启动前端开发服务器
cd web
pnpm dev

# 访问：http://localhost:3000
```

**选项 B：完整 Docker 部署**

⚠️ **重要**：首次部署必须先构建自定义镜像！

```bash
# 步骤 1：构建品牌化镜像（在项目根目录）
docker build -t langgenius/dify-web:brand-customization ./web
# 预计耗时：5-10 分钟

# 步骤 2：启动所有服务
cd docker
docker compose up -d

# 访问：http://localhost:80
```

如果跳过步骤 1 直接启动，会报错：
```
Error: manifest for langgenius/dify-web:brand-customization not found
```

详细部署说明：`dev-notes/交付报告-方慧敏/02-部署操作步骤.md`

---

## 📚 重要文档位置

- **项目总览**：`dev-notes/README.md`
- **快速恢复记忆**：`dev-notes/AI恢复记忆指南.md`
- **所有修改记录**：`dev-notes/修改记录/`
- **部署指南**：`dev-notes/前端修改后重新部署指南.md`

---

## 🎨 项目说明

### 项目名称
Dify 1.8.0 前端品牌化定制（YBP消防助手）

### 品牌色
- **黄色**：`#f2c823`（按钮、强调元素）
- **深灰**：`#615e5f`（容器、背景）

### 已完成的品牌化
1. ✅ 侧边栏品牌化（深灰背景 + 黄色按钮）
2. ✅ 发送框品牌化（黄色发送按钮 + 黄色输入文字）
3. ✅ Powered by YOMY（品牌标识）
4. ✅ 用户头像品牌化（深灰背景 + 黄色人物剪影）

### Git 分支
- **当前分支**：`feature/brand-customization`
- **主分支**：`main`
- **提交数**：3 次功能提交 + 1 次文档提交

---

## 💻 环境要求

- **Node.js**: 22+
- **包管理器**: pnpm
- **Docker**: Docker Desktop
- **操作系统**: macOS / Linux / Windows

---

## ❓ 常见问题

### Q1: 为什么需要重新安装依赖？
A: npm 包在不同操作系统上可能有差异，重新安装确保兼容性。

### Q2: 恢复需要多长时间？
A:
- 前端依赖安装：2-5 分钟
- Docker 镜像拉取：5-10 分钟（首次）
- 总计：约 15 分钟

### Q3: 如何验证恢复成功？
A: 访问 http://localhost:3000，查看是否显示品牌化效果（黄色按钮、深灰侧边栏等）

### Q4: Docker volumes 删除了会影响功能吗？
A: 不会。volumes 只是运行时数据，Docker 会自动创建新的空数据卷。

### Q5: Docker 部署时报错 "manifest not found"？
A: 这是因为没有先构建自定义镜像。解决方法：

```bash
# 在项目根目录执行
docker build -t langgenius/dify-web:brand-customization ./web
```

原因：`langgenius/dify-web:brand-customization` 是本地自定义镜像，不在 Docker Hub 上。

详见：`dev-notes/部署问题排查/2025-11-03-品牌化镜像不存在错误.md`

### Q6: 构建镜像时出现 EOF 错误？
A: 这是网络连接中断导致的。解决方法：

**方案1（推荐）**：直接重试
```bash
docker build -t langgenius/dify-web:brand-customization ./web
```

**方案2**：如果重试多次仍失败，配置 Docker 镜像源（详见部署问题排查文档）

成功率：约 80% 的情况下重试即可解决。

详见：`dev-notes/部署问题排查/2025-11-03-Docker构建镜像EOF错误.md`

---

## 📞 技术支持

如有问题，请查看：
1. `dev-notes/README.md` - 项目总览
2. `dev-notes/02-配置进度.md` - 环境配置详解
3. `dev-notes/修改记录/` - 所有修改记录和踩坑经验

---

*打包日期：2025-10-29*
*打包人：Stephen*
*项目版本：feature/brand-customization*
